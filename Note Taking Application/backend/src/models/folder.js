class Folder {
  constructor(folderId, folderName, userId, parentFolderId) {
    this.folderId = folderId;
    this.folderName = folderName;
    this.userId = userId;
    this.parentFolderId = parentFolderId;
  }
}

export default Folder;